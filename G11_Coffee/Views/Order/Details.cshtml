@model G11_Coffee.Models.Order

<h2>Order Details</h2>

<dl class="row">
    <dt class="col-sm-2">Order Date</dt>
    <dd class="col-sm-10">@Model.OrderDate.ToString("yyyy-MM-dd")</dd>

    <dt class="col-sm-2">Employee</dt>
    <dd class="col-sm-10">@Model.Employee.FullName</dd>

    <dt class="col-sm-2">Cafe</dt>
    <dd class="col-sm-10">@Model.Cafe.Name</dd>

    <dt class="col-sm-2">Total Amount</dt>
    <dd class="col-sm-10">@Model.TotalAmount.ToString("C")</dd>
</dl>

<h3>Order Items</h3>
<table class="table">
    <thead>
        <tr>
            <th>Product</th>
            <th>Quantity</th>
            <th>Unit Price</th>
            <th>Subtotal</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.OrderDetails)
        {
            <tr>
                <td>@item.Product.Name</td>
                <td>@item.Quantity</td>
                <td>@item.Price.ToString("C")</td>
                <td>@((item.Quantity * item.Price).ToString("C"))</td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="Print" asp-route-id="@Model.Id" class="btn btn-secondary" target="_blank">Print Invoice</a>
<a asp-action="Index" class="btn btn-primary">Back to List</a>